<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sale_order_view_search_inherit_quotation_mio" model="ir.ui.view">
        <field name="name">sale.order.search.inherit.quotation.mio</field>
        <field name="model">sale.order</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="sale.sale_order_view_search_inherit_quotation"/>
        <field name="arch" type="xml">
            <filter name="my_quotation" position="replace">
                <field name="campaign_id"/>
                <separator/>
                <filter string="My Quotations" name="my_quotation" domain="[('user_id', '=', uid)]"/>
                <separator/>
                <filter string="Quotations" name="draft" domain="[('state','in',('draft', 'sent'))]"/>
                <filter string="Sales Orders" name="sales" domain="[('state','in',('sale','done'))]"/>
                <separator/>
                <filter string="Create Date" name="filter_create_date" date="create_date"/>
                <filter string="Active" name="active_filter" domain="[('active', '=', 'False')]"/>
                <filter string="No Active" name="unactive_filter" domain="[('active', '=', 'True')]"/>
            </filter>
        </field>
    </record>
</odoo>